import { makeStyles } from "@material-ui/core";

export const useCurrencyTableStyles = makeStyles((theme) => ({
  currencyTable: {
    padding: "2rem 4rem",
    borderRadius: "2rem",
    backgroundColor: theme.palette.background.paper,
  },
}));
